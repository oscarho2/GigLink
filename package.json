{
  "name": "giglink",
  "version": "1.0.0",
  "description": "A social media platform for professional musicians",
  "scripts": {
    "backup": "node scripts/backup-database.js",
    "backup:schedule": "node scripts/schedule-backups.js",
    "restore": "node scripts/restore-database.js",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start:dev --prefix frontend\"",
    "start": "node backend/server.js",
    "build": "npm run build --prefix frontend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "puppeteer": "^24.19.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
