# Copy this to .env.local for local development with a separate Google OAuth client
# REACT_APP_GOOGLE_CLIENT_ID=your-development-client-id-here.apps.googleusercontent.com

# Note: .env.local is ignored by git and will override values in .env
# This allows you to use a separate Google OAuth client for local development
# that has http://localhost:3000 configured as an authorized JavaScript origin